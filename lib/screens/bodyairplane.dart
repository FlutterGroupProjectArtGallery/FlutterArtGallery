import 'package:flutter/material.dart';

class MyAirplaneBody extends StatelessWidget {
  const MyAirplaneBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('HERE COMES THE AIR PLANE!!', style: TextStyle(fontSize: 50)),
    );
  }
}
