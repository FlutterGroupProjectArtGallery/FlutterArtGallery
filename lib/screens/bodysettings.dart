import 'package:flutter/material.dart';

class MySettingsBody extends StatelessWidget {
  const MySettingsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome To Settings', style: TextStyle(fontSize: 50)),
    );
  }
}
